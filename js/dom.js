`use strict`;

// Travering DOM Elements

// getting parent element using the child element

// const txt = document.querySelector(`.text`);
// console.log(txt.parentNode);

// getting child elements of a parent node

const parentDiv = document.querySelector(`.title`);
console.log(parentDiv.firstElementChild);
// console.log(parentDiv.lastElementChild);
console.log(parentDiv.childNodes);
